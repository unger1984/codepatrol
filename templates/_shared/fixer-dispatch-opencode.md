- After the user selects an option in the Fix Decision Brief, use one platform task agent with the matching configured model tier: simple → fast, standard → default, complex → powerful. Pass exactly one finding, its selected option, and cited applicable rules; keep findings sequential unless the user approved an independent parallel group.
