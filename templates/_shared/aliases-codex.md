## Short Aliases

| User types | Resolve to |
|------------|-----------|
| `/fix` | `/cp-fix` |
| `/docs` | `/cp-docs` |

Note: `/review` is a built-in Codex command (working tree analysis). For full code review with CodePatrol, use `/cp-review` explicitly.
