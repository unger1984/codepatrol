- Use multi-agent tools only for the reviewer assignments already decided by the shared routing contract. Pass each reviewer only its assigned `prepared_context` excerpts, scope manifest, and blockers. Enable `spawn_agent`, `wait`, and `close_agent` only when needed for those dispatches, use `send_input` for follow-ups, and select the configured model tier for each reviewer role, including a powerful `architecture-reviewer` when common routing requires it.
