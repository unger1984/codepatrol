- When `requires_deep_compliance` is true, dispatch the powerful compliance reviewer first with only the
  reviewed files and minimal prepared context.
- After compliance is acceptable, use the platform's specialized review skills or Task tool to run only
  the needed quality passes in parallel when available.
- Select the configured model tier for each reviewer role.
