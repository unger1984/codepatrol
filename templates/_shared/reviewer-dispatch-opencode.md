- Use the platform's specialized review skills or Task tool only for the reviewer assignments already decided by the shared routing contract. Pass each reviewer only its assigned `prepared_context` excerpts, scope manifest, and blockers. Run independent quality passes in parallel only after compliance is clean, and select the configured model tier for each reviewer role, including a powerful `architecture-reviewer` when common routing requires it.
