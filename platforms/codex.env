ASK_USER=request_user_input
ASK_USER_OTHER_NOTE=explicitly in options
SKILLS_DIR=~/.codex/skills
RULES_DESCRIPTION=`**/AGENTS.md` only, root and nested, plus explicit language and codestyle sections inside them
RULES_LINE_1=1. **`AGENTS.md`** — root and nested (Glob `**/AGENTS.md`)
RULES_LINE_2=2. **`docs/plans/*-design.md`** — approved design documents when plan or compliance review needs them
RULES_LINE_3=
RULES_SOURCE=`AGENTS.md` files in repo scope (root + nested)
