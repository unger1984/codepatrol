name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Build skills
        run: ./install.sh build

      - name: Validate generated workflow skills
        run: |
          test -f skills/cpatrol/SKILL.md
          test -f skills/cpreview/SKILL.md
          test -f skills/cpfix/SKILL.md
          test -f skills/cpdocs/SKILL.md
          test -f skills/cprules/SKILL.md

      - name: Create release archive
        run: |
          VERSION="${GITHUB_REF_NAME#v}"
          tar -czf "codepatrol-${VERSION}.tar.gz" \
            skills/ templates/ platforms/ .claude-plugin/ \
            install.sh LICENSE README.md README.ru.md CLAUDE.md

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          files: codepatrol-*.tar.gz
